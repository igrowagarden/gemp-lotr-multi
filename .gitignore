# Python bytecode from the harness scripts
__pycache__/
*.pyc

# Captured wire data is re-capturable and large; keep the small canonical one.
src/dev/*.tmp

# Live harness locks, one file per running run. Runtime state, never content.
harness/.harness-locks/
